// imprimir la tabla del 10
let base = 10;
// limite de tabla
let limite = 5;

for(let i = 1; i <= 10; i++) {
        let resultado = base * i;
        console.log( base + ' x ' + i + ' = ' + resultado),
}